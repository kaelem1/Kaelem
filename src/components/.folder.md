# Folder: /src/components

1. **地位**：UI 组件层，提供可复用的界面元素。
2. **逻辑**：接收 props，渲染 UI，不处理业务逻辑。
3. **约束**：保持组件纯粹，状态管理交给父组件或 Context。

## 成员清单
- `/ui/`：shadcn/ui 基础组件 (button, card, badge, input, etc.)
- `/layout/`：布局组件 (Header, Footer)

**触发器**：一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
