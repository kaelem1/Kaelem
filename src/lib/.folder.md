# Folder: /src/lib

1. **地位**：核心工具层，提供数据处理和外部服务连接。
2. **逻辑**：封装 MDX 解析、Supabase 连接等核心功能。
3. **约束**：纯函数为主，不直接操作 UI。

## 成员清单
- `mdx.ts`：Markdown 解析，获取博客、阅读清单、作品数据
- `supabase.ts`：Supabase 客户端配置
- `utils.ts`：通用工具函数 (shadcn/ui)

**触发器**：一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
